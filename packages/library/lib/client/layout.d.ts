/**
 *
 * @param ratio 屏幕可视宽度所占比例
 * @param minWidth 最小屏幕宽度
 * @param maxWidth 最大屏幕宽度
 * @returns
 */
export declare function widthWithScreen(ratio: number, { minWidth, maxWidth }: {
    minWidth?: number;
    maxWidth?: number;
}): number;
